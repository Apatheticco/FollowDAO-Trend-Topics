---
name: create-trending-topic
description: >
  从原始素材或主动扫描热点，自动提取结构化字段，创建 Followin 热点风向标（TrendingTopic），并支持发布。
  三种触发场景：
  (A) 用户让"扫一下热点 / 找几个话题 / 批量创建今日话题" → 调 Followin MCP（metrics/news/signal/twitter）主动扫描；
  (B) 用户给一段素材（新闻/推文/描述）让创建话题 → 直接进评分；
  (C) 用户给一个标的或一句话（"查一下 REPPO" / "Aave 现在情况"）→ 多源补全资料再评分。
  ⚠️ 调研型 vs 建题型意图闸（v2.6.5）："看看/有没有 X 话题 / X 板块怎样 / 查一下 X"=**调研型→先 present 数据+评分+去重结论，问一句再建，禁止直接 create**；"建/补一个 X / 把 X 拆成 N 条 / 上线 X"=建题型→按 (B)/(C) 走。（2026-06-20 实测：把"看看 SK 海力士的话题"当建题直接 create，被用户"你创建了个啥"纠正。）
  全流程：采集 → 去重 → 评分 → 字段提取 + 标题优化 → 预览 → 创建 → tag 校验 → 数据核实 → 发布。
---

# 创建热点风向标

---

## ⚡ 执行卡（每轮照此跑，细节见 `references/` 详章——文末索引）

> 90% 的扫描照本卡执行即可；遇到边界/判定再按文末「📚 详章索引」翻对应 references 文件。本卡是「跑什么 + 怎么并行」的**单一事实源**。完整事故经过/实测数据/版本演进归档在同目录 `LESSONS.md`（按需查阅，不随每轮加载）。

### 0. 开跑前（铁律）
```bash
date '+%Y-%m-%d %H:%M:%S %Z (UTC%:z) | Unix: %s'   # 必跑，算 cutoff_4h/24h
tail -1 $HOME/.trend-scout/last-refresh.txt 2>/dev/null   # 必跑，上轮真实时间戳（单一文件、不带日期，跨天天然连续）
# 跑完一轮在结尾追加：  echo "$(date +%s)000" >> $HOME/.trend-scout/last-refresh.txt
```
> 🗂️ **时间戳用单一 append-only 文件 `$HOME/.trend-scout/last-refresh.txt`（铁律 v2.6.5 — 2026-06-20：旧按天命名 `-YYYYMMDD.txt` 跨天会读到当天空文件，算出乱码间隔，本会话连坑 3 次）**：文件名**不带日期**，每轮结尾 `echo "$(date +%s)000" >>` 追加，开跑 `tail -1` 取最后一行。禁止按天分文件、禁止只读当天文件。
判模式（v2.8.8 — 2026-07-07 用户口径「每天第一次=首扫，后面=刷新」）：**当天第一轮 → 🟢首扫(24h)；当天已跑过 → 🔵刷新(4h)**。判据客观化：`last-refresh.txt` 最后时间戳与当前 date **跨天 = 当天第一轮**，禁止凭会话记忆。用户显式说"跑首扫/跑刷新"以用户为准。~~旧"06-10 点首扫、之后刷新"钟点判据作废~~（同一天多次首扫浪费、深夜首轮误判刷新漏隔夜料）。今日 status=0 发布 0 条 → 强制首扫（兜底照旧）。

> 📸 **收尾双留档（铁律 v2.8.3 — 效果评估地基）**：每轮收尾必做两笔追加（**单一 append-only 文件**，同 v2.6.5 教训不按天分文件）：
> - **B0 快照** → `~/.trend-scout/snapshots.tsv`：本轮 B0 全量逐条 `run_ts<TAB>id<TAB>domain<TAB>status<TAB>created_at`。API 只给当前态、无变迁历史——没有跨轮快照，采纳率/打回率/误撤率全算不出。
> - **写操作日志** → `~/.trend-scout/actions.tsv`：本轮每笔写操作逐条 `run_ts<TAB>action(create/cull/merge-keep/merge-hide/update/publish/**restore**/rulefix/note)<TAB>id<TAB>备注`（restore=v2.9.7 起的误撤恢复 status→0）。区分"我做的"和"系统/团队做的"全靠它。
> - 周复盘用这两份算指标，见 `references/weekly-review.md`。

> ⏱️ **跨轮间隔禁止凭会话记忆（v2.6.0 — 2026-06-12 实测：差点把昨天数据当"5 分钟前"复用）**：上轮何时跑的，**一律以 `$HOME/.trend-scout/last-refresh.txt` 最后一行时间戳 − 当前 date 计算**，"我记得刚跑完"不算数——compact/隔天续接后会话记忆必然失真。
> - 实际间隔 **≤30min** → 可复用上轮价格快照/B0，只补增量
> - **>30min** → 价格快照重拉
> - **跨天** → 价格 + B0 去重池**全部作废重拉**（v2.5.4 同源：旧池子当当下是最危险的错）

### 1. 并行批次表（同一格 = 同一 turn 内并发；followin 单批 ≤5 防掉线）

> 🧱 **两条硬约束铁律（v2.9.4 — 2026-07-11 实测）**：① **`metrics` 的 `keywords` 每次硬上限 5 个**——超了静默截断到前 5、返回 `warning: keywords truncated: N→5`，多的直接丢（07-10 同调用 10 个还全返回，隔夜 API 收紧）。10 币拆 2 批、板块矩阵每批 ≤5，**别一次塞 10（看不见的漏扫）**。② **B0 `list_trending_topics` 的 `date_range` 必须含今天**——`start_date/end_date` 要覆盖当前 date（跨天写 `昨天~今天`），否则漏当天新建话题、误报"8h 池为空"（07-11 实测栽坑）。③ **B0 `limit=100`（API max）+ 满额守卫（v2.9.8 — 07-17 首扫 total=80 正好顶到旧 limit=80）**：返回 `total == limit` 即视为**可能截断**，必须缩短 date_range 或分 `status` 拆两次拉齐，禁止拿顶格结果当全量（同 keywords 截断，是"看不见的漏扫"）。

> ⚠️ **数组参降级闸（v2.9.13）**：本表所有 `metrics` 调用范式默认数组参；**会话注册表剥空时（报 -32602）整表改 query 形态**——`metrics(query="<实体空格串> price snapshot", asset_type=...)`，实体 ≤5/批照旧（例：板块批1 → `query="NVDA AMD AVGO TSM TSLA price snapshot", asset_type="tradfi"`；代价=categories 锁不住、macro `kw_not_canonical` warning 属噪音可忽略）。判型与处置见下方「🔧 通道故障分型速查表」。
>
> 🚀 **单 turn 齐发铁律（v2.8.9 — 2026-07-07 实测：串行跑一轮 15min，用户「优化一下」）**：刷新模式**全部批次（B0 console + B1/B2 followin + okx 涨/跌/OI + A1 子进程）一律在同一条消息里齐发**，不分先后波——各批间无数据依赖，分波纯属浪费；墙钟=最慢一路（子进程 ~2-3min）→ 全轮 4-5min。**三大违规姿势**：① 单 call 单 turn 逐个调、call 之间夹叙述（主犯，10 个串行回合 ≈6-8min）；② A1 子进程放末尾才起（长杆必须第一波起，§1 原有规则重申）；③ 首扫因 followin 单批 ≤5 需 4 波，但 **B0/A1/okx/TG 与 W1 同发，W2-W4 只等 followin 前一波返回即续发，不等其他通道**。A1 dump-only 子进程**一律 `model=haiku`**（机械 dump 不需要大模型，实测 opus 103-178s，haiku 显著更快）。收尾三合一：处置写操作 + 留档 bash + 报告同 turn 出。

**🔵 刷新（4h）— B0+B1+A1 同波并发 + B2 + 1 console**
| 批 | server | 并发 calls |
|----|--------|-----------|
| **B0**（与 B1/A1 同波，**不单开一波**——console 与 followin/Agent 不同服务器、无依赖，只需在 0a 评分前到位）| console | `list_trending_topics(24h, 全状态, **limit=100** + total==limit 满额守卫，见上方铁律③)` |
| B1 | followin | 10币 metrics（**`categories=["market"]`**）｜ 跨市场 metrics（金=`XAUT`@crypto / 油+美元=`["CLUSD","DXY"]`@tradfi，均加 `categories=["market"]`；见 quirk⑨⑩）｜ news(crypto market 4h) ｜ news(listing/解锁/SEC 4h) ｜ **〔第 5 槽空出，按需补漏网币第二轮核〕** |
| B2 | followin | **TG exploit 探针（1 条，v2.6.6）** ｜ news(美股 query 兜底, 不传 asset_type) ｜ Wave5 鲸鱼 ｜ Wave5 上币 ｜ 〔空〕 |
| A1 | Agent×2（与 B0/B1 同波起）| **刷新只 2 栈**：主 `2046422494643687464` + 科技AI `2051854001608724654`（大师 list 刷新砍——4h 几乎每次"无新推"，移首扫；明确"看大师"才破例）。**子进程 prompt 用精简版**（见 §1 末「子进程范式」）|

> 刷新 **砍掉的死调用**（实测）：
> - **❌ tradfi 头条 news 砍**（quirk④：`asset_type="tradfi"` 每次返 0；美股 query 兜底已覆盖芯片/宏观头条）。
> - **❌ Wave5 ETF query 砍** — `spot` 总被解析成 Spotify / degraded；ETF flow 改由 W2 media query 覆盖（`news(query="bitcoin ethereum ETF flows inflow outflow")`，TG bot 已随 v2.6.6 裸 feed 一并砍）。
> - **❌ Wave5 上币 query 刷新砍**（4h 内永远返 9 天前陈旧 HACK ETF），只首扫留。
> - 不跑 signal / popularity / 国债 / spx ｜±15% 漏网币第二轮核。

**🟢 首扫（24h）— 4 波 followin 塞满 + 1 TG exploit 探针 + 3 Agent + 1 console（窗口 1d 替换 4h）**

> ⚡ **批次铁律（v2.5.1 — 2026-06-06 复盘：上次 6 波串行跑了 7min）**：每波**严格塞满 5 个 followin**（≤5 防掉线），4 波打完所有价格/news，别欠装、别为 1 个 call 单开一波。spx 必 fail → SPY/QQQ 兜底**预判着塞进 W1**，不要等失败再补一波。
>
> | 波 | 5 个并发 followin call | 备注 |
> |----|----------------------|------|
> | **W1 价格核心**（**+ B0 console + A1 三栈 同波起**）| ①10币(market) ②金 XAUT(crypto/market) ③油+美元+spx `["CLUSD","DXY","spx"]`(tradfi/market) ④SPY+QQQ+VIX(tradfi/market，spx 兜底**预判直塞**) ⑤crypto market news(1d) | **B0 + A1 三栈 list 子进程同波并发**（console/Agent 不占 followin 的 5 槽）|
> | **W2 事件/链上 news** | ①listing/解锁/SEC/漏洞 news ②美股 query 兜底(不传 asset_type) ③Wave5 鲸鱼 ④Wave5 上币 ⑤Wave5 解锁/财库 | 全 news；**tradfi 头条已砍**（quirk④ 返 0，②兜底覆盖）|
> | **W3 板块+宏观（前半）** | ①板块批1[NVDA,AMD,AVGO,TSM,TSLA] ②板块批2[MSFT,GOOGL,META,AAPL,AMZN] ③板块批3[MU,SNDK,WDC,STX,LITE] ④板块批4[AAOI,GLW,COHR,ASML,AMAT] ⑤**econ 日历（前瞻：传 `date_from`=今天 `date_to`=+5d，见 quirk⑫）** | **板块矩阵 6 批×5（v2.9.8 — 修正 v2.9.4 改漏的本表旧"3×10"，keywords>5 静默截断=漏扫）**；**⚠️美股时段闸：US 休市/盘前（周末或 ET 收盘后）→ 板块矩阵=上一收盘陈旧，①~④ 跳过标"盘前取上收盘"，开盘后再拉**。TSLA/AMZN 已并入批1/2 覆盖 Wave2C |
> | **W4 板块（后半）+扫尾** | ①板块批5[LRCX,KLAC,CRCL,COIN,MSTR] ②板块批6[HOOD,SOFI,PLTR,RKLB,MRVL] ③国债 DGS10/DGS2(macro) ④跌榜 okx `market_filter(sortBy="chg24hPct", **sortOrder**="asc")` ⑤earnings 日历 `query="earnings calendar this week", categories=["fundamentals"], asset_type="tradfi"`（周一/财报周；非财报周此槽给 按需 `CPIAUCSL/UNRATE` 或 ±15% 漏网币第二轮核）| 板块批5/6 受同一美股时段闸。涨榜 `biggest gainers` **默认砍**（penny + min_market_cap source_dead，quirk⑪）|
> | **W-TG** | **TG 1 条 exploit 探针**（`query="hack exploit drained bridge vulnerability"`, telegram, 1d）—— v2.6.6 裸 feed 已砍 | 单 call，可并进 W2 空槽，不必单开波 |
>
> A1 第 3 栈 = **大师 list `2051856808348987697`**（首扫必跑；刷新砍）。3 栈子进程 ~30-45s 是长杆，**W1 就起**和 W2-W4 并行跑完，别落关键路径末尾。
>
> **🤖 子进程范式（提速）**：list_timeline 子进程 prompt **只让它"按时间倒序 dump 最新 N 条 + 原始时间戳"，不要让它算 velocity**（velocity 计算吃 ~15K token/30s+，主进程粗排即可）。每条格式 `[MM/DD HH:MM] @author | 👍likes 🔁rt | text前130字`。N：首扫 主15/科技12/大师8，刷新 主10/科技8。**🚫 三栈禁止主进程直调 `list_tweets`（v2.9.13）**——07-22 实测 limit=25 返 60.8KB 超限落盘只剩 preview = 该栈实际半盲，一律走 A1 子进程 dump。
>
> **🔧 通道故障分型速查表（v2.9.13 — 整合 v2.9.11a~d/9.12，事故叙事全在 LESSONS 对应版本行）**：
>
> | 错型 | 根因/判型 | 处置（按序） | 计熔断？ |
> |------|----------|------------|---------|
> | stdio「断开」（tradingview 等）| `~/.claude.json` 该 server `type:"stdio"` = 本地进程死 | `ToolSearch select:<工具名>` 触发 harness 重拉进程，一步复活；免重开 session/免动配置 | 否 |
> | `-32602 … want "null, array"` | **会话注册表把数组参剥空 `{}`**（curl 直连实锤服务端 schema 完整无责，**勿转 dev**；ToolSearch 重载无效）| ① 实体写进 `query` 省掉数组参（见上方降级闸）→ ② 同族备用 server（**存在性每会话实测**——07-22 followdao-test 已断开，不存在则跳过此步）→ ③ 降级梯 | 否 |
> | `invalid during session initialization` | SSE 重连 init 握手窗口，~40min 自愈（07-21 实测；07-22 晚重试即通二验）| 同轮重试 1 次 → 仍败本轮走 okx，下轮正常再探 | 否；**跨 ≥2 轮同错**才升死锁 |
> | 持续死锁 / fmp 403 / `degraded` ×2 | 主进程连 2 次同型失败 **且** 子进程逃生舱也败 **且** 备用 server 也败（备用不存在则前两条即判）| **会话级熔断**：后续轮不再探路直走兜底（TG 探针→news 全源 query，实测捞全 BONK/OSTIUM/Summer.fi；tradfi 报价→yahoo/okx；metrics→okx+子进程）；**下次首扫重探 1 次**；报告固定标「⚠️X 通道熔断中·兜底=Y」 | 是 |
>
> **🛟 价格源降级梯（followin.metrics 挂时）**：metrics 报数组参错（先按上表处置）或整体不返 → **crypto 走 okx `market_get_ticker(instId="BTC-USDT")`（单标的，10 币=10 call）；tradfi/指数/油金走 web 搜**。`okx` 的 `open24h` 算 24h 涨跌。yahoo/tradingview 偶发 SSL 证书错（07-08 实测 `tradingview yahoo_price` 全会话稳定，可用）。**tradfi 403 会话级熔断（v2.9.1 — 07-08 连续 3 轮 fmp 403 仍每轮先打 followin=白烧）**：followin tradfi 报 fmp 403（source_dead）→ **本会话后续 tradfi 报价直接走 yahoo_price/okx，不再先打 followin 探路**；下一次首扫重探 1 次，恢复则解除熔断。
> **🥇 个股价格锚=正股优先（v2.8.7 — 2026-07-07 用户指令「不要用代币化股票，直接用 Followin 查正股」）**：个股/板块行情话题的标题价格一律锚**正股**——`followin.metrics(asset_type="tradfi")` 直查，非美股带正确后缀（韩 `.KS/.KQ`、日 `.T`、港补零 4 位 `.HK`；公司英文名也可解析，"SK Hynix"→000660.KS）。**代币化股票（OKX SKHYNIX/MU/TSLA-SWAP 等）不作主价格锚**——与正股有真实点差（实测 07-07：SK 海力士代币化 -4.8% vs 正股 -6.1%，锚错失真 1.3pt）；仅两用途：① 正股闭市时段的连续行情参考（标题/正文须显式写明"代币化"）；② crypto 域联动叙事。正股 metrics miss 才走 v2.6.7 降级梯（WebSearch→tradingview→Google Finance WebFetch），禁止先抓代币化凑数。**domain 联动：正股锚的个股/板块行情话题一律 `domain="tradfi"`**（韩/日/港正股与美股同待遇；只有叙事主体是代币化标的/链上联动时才 crypto）——SK 海力士 10423 建在 crypto 被用户「类型记得标记 Tradfi」纠，重建 tradfi 10000002103，同三星 10417→10000002102 前例。
> **🛟 news 通道降级梯（v2.8.4 — 2026-07-04 AUTO 实测：followin 主 session 重init 时 metrics/news 全拒，但 Agent 子进程各起新 session 仍活）**：`followin.news`/TG 探针在主进程报 `session initialization` → **改用 Agent 子进程代跑**（prompt 让子进程 ToolSearch 加载 `followin__news` 后按同参数调用、只回传结果）。子进程独立 session 能绕过主 server 抖动（三栈 list 一直靠这个活）。子进程也失败才算该通道真缺失。
> **🔌 通道会话级熔断（通用铁律 v2.9.8，判据与兜底见上方分型表第 4 行）**：熔断=会话级状态，不算半盲但必须可见；已知两种 TG 死法：主进程 `status:degraded/total=0`、子进程 `sources` 数组 schema 拒（任一复现 2 次即熔断，兜底 news 全源 query）。**判死前必试同族备用 server（v2.9.11——没试过备用就判死=臆测，曾白降级 3 天）**，备用不存在（每会话实测）则免此判据。
> **🚨 半盲熔断铁律（v2.8.4 — 同源实测：followin 挂那轮只 okx 补了价格，news/TG 整条哑掉却照报"干净干轮"=误导）**：任一**必跑通道**（news 事件面 / TG exploit / 三栈 list）失败**且降级梯也补不齐** → 该轮结论**强制标「⚠️ 半盲扫·结论存疑」，禁止报"干净干轮/无候选"**（缺的是"看没看到"，不是"有没有"）。报告显式列**哪条通道缺、缺了什么维度**。AUTO 模式尤其致命（无人复核），半盲轮必须留醒目标注等人补扫。**补扫在飞不预支（v2.9.1 — 07-08 主 list 重试未归即报"非全盲"，赌对但流程违规）**：某必跑通道的重试/子进程还在飞时，报告**不得**先写「非半盲/全通道跑通」——标「⚠️N 通道补扫中·结论待确认」，待其返回后再补记升格。
> **📈 movers 异动通道（v2.8.6 — 2026-07-04~07 实测）**：全市场涨跌榜/OI 异动**主走 okx**：`market_filter(instType="SWAP", sortBy="chg24hPct", sortOrder="desc"/"asc", minVolUsd24h≈1500万)` 涨跌各一 + `market_filter_oi_change(instType="SWAP", bar="1H"/"4H")`。**参数三坑**：① `instType` 必填；② sortBy 枚举是 **`chg24hPct`**——传 `priceChangePercent` 直接 400 Bind Arguments（实测我曾据此误诊"OKX 挂了"，其实是自己传错参——先读 schema 再下"通道死"结论）；③ capabilities 里 swap/spot/account 等 `MODULE_FILTERED` 是**交易模块被关**（只读部署设计），market 模块 `enabled` 照常，别当"下架"；④ **排序方向参数名=`sortOrder`（v2.9.13）**——07-22 传 `sortType` 被 API **静默忽略**返回默认涨榜，误报"跌榜参数不支持"并写进留档（**未报错的错参比报错更险**；本块②早写对了参数名，下结论前先查自家规则）。**followin.metrics movers 不可用**：query='biggest gainers'/'涨幅榜'/'最活跃'/'市场异动'（help 自己宣传的写法）实测全 total=0 且错路由 FRED——advertised-but-empty，涨跌榜别指望它。okx 挂时 fallback `tradingview top_gainers`，但**单源不可信**（实测币安现货口径 +1.65% 封顶失真、险把 HMSTR +83% 漏成"干轮"）→ movers 结论 okx 为主，与 news 面对不上时双源交叉再定，单源静默不下"干轮"结论。

### 2. 候选处置路由（status × create_at 年龄 → 走哪条规则）

> 🔁 **轮内执行序铁律（v2.8.5 — 2026-07-07 用户指令「先盘存量再补新」）**：每轮固定四步、顺序不可倒——**①扫热点**（§1 批次全跑）→ **②盘 8h 池**（从 B0 圈出 `create_at ≤8h` 全量话题，status 0/2 都要）→ **③处置存量**（改/合并/撤**全做完**：≤8h 上线题退场复查 v2.6.8、≤8h 审核题质量复审 v2.7.7、≤6h 重复合并 v2.7.3、时段闸内改价 v2.7.8）→ **④最后才补新题**（§3 评分建题）。**禁止先建后盘**：补题的去重基准必须是"处置后的池子"——存量里能升级承接的（≤8h 同事件 → update 老题）就不新建；先建再撤存量会造成同事件新旧并存、自我打架。三件套**报告**顺序仍 🆕补 最前（用户最关心），**执行**顺序补题最后。AUTO 与手动扫描同样适用。

> 🕗 **8h 管理视野闸（铁律 v2.7.1 — 2026-06-26/27 用户指令收口）**：**扫描时只管 `create_at ≤8h` 的话题**。**`create_at >8h` 的话题一律不管——不复查、不脱锚撤、不升温改、不进三件套决策、不在简报里提处置**，让其自然沉底，当前催化一律由**新话题**承接。8h 是单一管理边界（建/改/撤/复查全部以它为界），不再用 24h。（2026-06-26 我提议撤 22h 前脱锚的 2004/2010 被顶回"超 8h 不用管"。）

| status | create_at 年龄 | 处置 |
|--------|------|------|
| **0 上线** | ≤8h | 数据偏差 ≥10%→升温 update title；>25%→撤(status=3)；>50% 反向→强撤（= 下方近 6h 退场复查）；**A≤1 灌水类 → 已上线也直接撤，不豁免（v2.9.2）**：产品升级/技术里程碑/链上 vanity 数据/已落地的上线宣传+奖励池均撤；⚠️ **例外：带明确未来日期的空投/上线预告可留**（GRVT 7/21 上线+28%空投=有日期的可交易预期，用户 07-08 分级）|
| **0 上线** | **>8h** | 🚫**默认不管**（不复查/不脱锚撤/不升温/不提）；当前行情 → 新建一条。**破闸例外全清单（v2.9.8 集中——此前散落 4 处执行时靠拼凑）**：① **事实错误**（v2.9.4：硬数字/硬定义词失实，如"涨3.7%"实为收跌、"明日挂牌"已过期；仅限客观可证，行情漂移不算）；② **盘中反转错向**（v2.9.6：早盘"XX下挫/上涨"题收盘方向反转=①的高频子类，fact-check 必校方向）；③ **碎片/重复卫生**（v2.7.3+v2.9.6：同事件多条合并、单名 vs 板块冗余隐单名——卫生问题非追新）；④ **tag 卫生**（v2.9.5：顺手 force-set 已知 tag）。8h 闸防的是"追新脱锚"，不豁免"错的东西挂在前台" |
| **2 审核** | ≤8h | 0a.5 预过滤 → ✅发 / 🟠改 / 🚨撤 / 🟡待核（表 3 必列）|
| **2 审核** | **>8h** | 🚫**完全不管**（从简报剔除、不再 push、不撤不改）|
| **3 hidden** | — | 不重建、不分析 |

> **🔁 近 6h 上线话题退场复查（铁律 v2.6.8，受 8h 闸约束）**：每轮从 B0 挑出 `status=0 且 ≤6h` 逐条复查——① 脱锚/失实/过期 → `status=3`；② 数据旧但仍成立 → update；③ 仍准确 → 不动；④ **A≤1 灌水类上线不豁免直接撤（v2.9.2，系统推上线≠质量背书；meme 除外 v2.9.7）**。**灌水分级**：撤=产品升级/技术里程碑/链上 vanity/已落地上线宣传；留=**带明确未来日期的空投/上线预告**（判据："读者能否据此提前布局一个日期事件？能=留"）。>8h 不进此复查。系统批只扫明显错误，自建逐条过。（base case：LESSONS v2.9.2）

> **🔎 近 6h 审核池质量复审（铁律 v2.7.7）**：与退场复查配对——每轮从 B0 挑出 `status=2 且 ≤6h` 逐条审，不合格直接 `status=3`（系统批与自建一视同仁）。**不合格四类（任一即撤）**：① **A≤1**（无清晰可交易标的被定价：财库流水/产品调整/DAO 治理/KOL 口水/政治披露）；② **链上红线**（匿名对赌/机构例行托管流水）；③ **事实红线**（硬定义词失实/隔日数进标题）；④ **灌水噪音**（空投/上币类；~~小币 meme~~ 已废→v2.9.7）。合格=有清晰标的+强催化。>6h 不进。（基准案例：LESSONS v2.7.7）
> - **🙈 >6h 仍卡 `status=2` 的自建题 = 一律忽略（铁律 v2.9.18 — 2026-07-30 用户「超过6小时的，就自己忽略」）**：**不复查、不跟进、不进报告、不建议用户去催**。判据只看时长，不看是不是我的题、也不看内容多好。**由来**：v2.9.17 落库后我在报告结尾列了 `10000002349`(卡7h)/`10000002273`/`10000002276`(均超48h) 问"是不是需要团队看一下"——用户当场收口。**这类卡滞既不在我可控范围、也已被 v2.7.7「>6h 不进」覆盖，再提就是噪音**（同 memory `feedback_stop_reraising_dismissed_items` 的两问自检：「用户是否已表过态」「这件事我能不能推动」）。

> **🐸 meme 币默认保留（铁律 v2.9.7 — 2026-07-17 用户指令「默认保留」，推翻 meme 适用 A≤1 灌水闸）**：**`A≤1 行情关联度闸` 与 `④灌水噪音` 对 meme 币一律不适用**。meme 题**默认留**，撤 meme 必须有**正当理由**，且只限三类：① **同标的重复**（只留最全/最新 1 条，如 INDEX 的 10630 vs 10632 留后者）；② **已证伪**（rug/假消息/被官方否认）；③ **事实错误**（硬数字失实）。
> - 🚫 **明令禁止的撤 meme 理由**：「小币」「无叙事锚」「短时波动」「市值小」「纯投机」——这些**不再是撤的依据**。meme 的热度本身就是可交易信号（散户在打），A 闸防的是"新闻大但没标的"，而 meme **有标的（币本身）+有价格动作**，从来不该进这个闸。
> - **由来**：CASHCAT（v2.9.0）+ BRIAN/INDEX/TENDIES 等连番误撤、TENDIES 被团队自行恢复回 status=0，用户两次点名后定为默认保留（详 LESSONS v2.9.7）。
> - **判据一句话**：meme 只问"是不是同一个币重复了/是不是假的/数字错没错"，**不问"值不值得"**。
> - **📐 适用范围=「热点小币」全类（v2.9.8 补口径——v2.9.7 只写 meme 没定义边界，ZIG/EVAA/DRV/AKE 算不算全靠临场猜=误判温床）**：本铁律覆盖 **meme + 小市值山寨 + 新上所币**，判据是**「有无真实价格动作」**——大涨/大跌（±30%+）、大振幅、放量、市值里程碑/新高、合约 OI 激增，**任一命中即默认保留**（EVAA 暴涨64%、DRV 上币放量、AKE +61.8% 全算）。**仍走 A 闸的只剩**：纯公告无价格动作（合作宣布/产品上线但币价没动）、纯 KOL 口水无标的。拿不准 → 留。

> **🔀 同事件 ≠ 同角度：技术根因题与市场反应题不合并（铁律 v2.9.15 — 周复盘#3 唯一越线项逼出）**：合并去重（`merge-hide`／上方 meme 三类①「同标的重复」／§0 破闸例外③「碎片重复卫生」）**只针对真重复**。同一事件下若两条分属**不同切面**，即便标的相同也**一律不合并、两条并存**。
> - **判据（两条都满足才算"不同角度"，任一不满足=真重复该合）**：① **标题主语不同**——一条讲**技术/事实根因**（协议、漏洞机理、被盗数量、攻击手法），另一条讲**市场反应**（币价涨跌、情绪、爆仓、资金流）；② **各自有独立数据锚**——不是同一组数字换个说法，而是各自指向不同的源和量纲。
> - **反例（我的误判，周复盘#3 实测）**：`10745`「Wanchain Cardano桥遭攻击，NIGHT暴跌后市场情绪急转」被我 merge-hide、保留系统的 `10730`「Wanchain跨链桥遭签名重放攻击，5.15亿枚NIGHT约1000万美元被盗」——理由是"后者数字更硬"。**团队把 10745 恢复了，终态两条同为 status=0**。10730 主语=漏洞机理（签名重放/5.15亿枚），10745 主语=价格与情绪，两条主语不同、锚不同 → **本就不该合**。
> - **⚠️ "数字更硬"不是合并理由**：数字硬只说明那一条**质量更高**，不说明另一条**是重复**。想合并前先问「这两条的主语是不是同一个东西」，而不是「哪条写得更好」。
> - **拿不准 → 两条都留**（与 meme 铁律同款倾向：合并的下行风险=误撤，保留的下行风险只是稍显冗余）。

> **🕰️ tradfi 价格复查的时段闸（铁律 v2.7.8 — 2026-07-01：休市时段别做无用改价）**：复查/复审 **tradfi 价格类话题**（个股/板块涨跌）时，**先判美股自话题建立后有没有经过一次新 regular session**：
> - **US 休市 / 盘后 / 盘前 / 周末**（上次收盘后无新交易）→ 话题里的**收盘价数据仍是当前值、不脱锚**，**别改**（改了也是同一个数）。别凭"3-6h 前建的可能旧了"就空改——**先实测**（拉 metrics 对比），实测一致就不动。
> - **只有话题建立后经过了新的开盘 session** → 价格才可能变，才拉最新价复查、按需改。
> - **判据**：`话题 create_at` 之后是否跨过一次美股 09:30–16:00 ET 交易时段。实测 07-01 盘后（22:15 ET，无新 session）复查 2063/2064：COIN -3.6%、SPCX +4.1%、RKLB +3.6% 与实时 metrics 完全一致 → 无需更新。
> - ⚠️ 例外：**盘后/盘前大幅异动**（财报、突发）会让盘后价偏离收盘价——这种有 after-hours 大波动的才需盘后改；常规休市静默则不动。crypto 24h 交易**不受此闸**（随时可能脱锚，照常复查）。

> **🧩 系统碎片/重复检测 + ≤6h 主动合并（铁律 v2.7.3 — 2026-07-01 用户指令，从"列建议"升级为"主动合并"）**：每轮扫 B0 **主动跑两项健康检测**：
> - **① 内容重复（同事件，含不同标题）**：同一催化/同标的被拆成 ≥2 条（**不限同标题——不同措辞的同事件也算**，如 2026-07-01 Circle 崩盘的 10240/10241/10243/10246/10248/10254）：
>   - **`create_at ≤6h` → 主动合并（动作，非建议）**：留**最全/最干净 1 条**（无则我方新建打包，如 Circle 的 2066），其余重复逐条 `update_trending_topic(status=3)` 下架。**前提是各重复标题互异可唯一定位**；标题全同的走②。
>   - **`create_at >6h` → 只列不擅动**（超 6h 合并范围；系统批列出交 dev/团队）。
> - **② 同标题重复刷屏**：同一标题被系统批刷 ≥3 条（如 2026-06-30 MSTR「数字信贷框架变现BTC」×18）→ **工具清不掉**（`update` 按标题匹配命中多条直接报错、无 ID 入口，见下「console MCP quirk」）→ 收尾标「系统重复 N 条」**交 dev 后台去重**。
> - 检测**不因 >8h 豁免**（碎片/重复是"卫生问题"、非"追新脱锚"，8h 闸只管后者）。合并动作范围 = **≤6h + 标题互异可定位**；≤6h 但同名、或 >6h 的，只列交 dev。
> - **🆕 全池摊开 + >6h 冗余/反转破闸（v2.9.6）**：① 去重审计**必摊全池 status=0+2**（不只 ≤8h 窗），收尾固定动作（§3 第 6 块）；② 同事件「单名 vs 板块全景」冗余 → **主动隐单名留板块**（>6h 也动，标题互异可定位即可，团队本就在自去重）；③ 盘中反转「错向老题」→ 破 8h 闸下架——**fact-check 拿收盘价对标题校方向，反了就清**（不只校幅度）。同名多条仍交 dev。（实测案例：LESSONS v2.9.6）

### 3. 评分 → 三件套 → 建/核/发
- **0b.0 时效硬过滤**：published_ts 超窗（首扫>24h / 刷新>4h）→ 直接砍，不进评分。
- **四维评分** A 行情冲击(0-3)+B 讨论热(0-3)+C 稀缺(0-2)+D 紧迫(0-2)；max(A,B)≥2 才排序。P0≥8 / P1=6-7 / P2=4-5。🚧 **行情关联度闸（v2.7.0）：A≤1（无清晰可交易标的被该催化定价）→ 即使 B 拉满也不作 P0/P1 独立立项，降级背景舆情**（防"新闻大但价格驱动弱"的料误判主推，详见 0b 步 A 维）。**⚠️ meme 币不适用本闸（v2.9.7）**——meme 有标的(币本身)+有价格动作，从来不该进 A 闸，默认保留，见 §2 meme 铁律。
- **🔍 "印证型"候选回查铁律（v2.6.4 — 2026-06-18 复盘：SK 海力士被埋成苹果涨价的印证脚注，漏判独立 P0）**：当一条料被你当成"另一条候选的背景印证/同向佐证"写进正文时，**回头单独给它打一次分**——若它本身 ≥P1（有独立标的+独立催化+独立行情），**必须拎出来独立立项**，禁止当配角的背景板。常见倒挂：把上游主角（HBM4E 样品首发的 SK 海力士）降级成下游色彩（苹果因成本涨价）的印证。判据一句话："这条料拿掉那条主候选，它自己还站得住吗？站得住就独立立项。"（同类病见 LESSONS「6/8 抢报干轮」）
- **三件套主报告**：🆕表1 补充（**不设数字 KPI——v2.8.2 退役旧"首扫12/刷新8"目标**：系统自动批饱和 + A≥2 闸后真缺口通常 0-3 条/轮，数字目标只制造扩窗灌水。口径=**全源扫完 + 过闸候选如实报，0 条即干轮**；大事日却 0 候选先对照 §1 自查通道漏跑）｜🔧表2 更新｜🚨表3 撤回+在审池处置。
- **0️⃣ 素材去重前置（铁律 v2.6.2 + v2.6.9 8h 活动时效闸 — 2026-06-16/06-25 用户指令）**：用户提**单条素材**要建话题时，**create 前必先 `list_trending_topics` 扫 status=0(已上线)+status=2(审核中)**，按相关话题的**最后活动时间（`created_at` 或最近一次 update，取最近者）**判去留：
  - **≤8h 内有同标的/同事件 → 优先 `update` 老话题**（更新数据点/标题/keywords），不新建重复；
  - **>8h（哪怕被更新过、只要最后活动仍 >8h）→ 不再考虑 update，直接新建一条**（v2.6.9 — 老题已积累 heat/一致性，>8h 追改=破坏一致性+内耗，当前催化由新话题承接；案例：LESSONS v2.6.9）；
  - 8h 内无相关话题 → 直接新建。
  （这是 B/C 素材入口的固定第一步，与扫描模式的 0a 去重同源，但对单素材输入强制前置。同源规则见下方「升温硬规则适用范围」，两处阈值统一为 8h 活动时效。）
- **📅 预告→结果题生命周期（v2.9.13 — 固化 CPI/谷歌财报节奏）**：带明确日期的预告题（财报/宏观数据/上市/分叉），**结果落地后建结果题承接 + 预告题 `status=3`**——此类自建自撤=计划内生命周期，**非误撤**（周复盘按此口径归类，勿计入质量失误；案例：CPI 10000002140→10000002154、谷歌财报 10000002229）。
- **📰 假新闻两段式（v2.9.13 — 07-22 双案例固化）**：重大爆料**单源不建**（v2.5.3 照旧，押后待交叉）；一旦当事方**官方否认**，「辟谣」本身升格为可建事件。判据三条齐备即建：① 传播面广（A 级账号转发/万级阅读）② 官方实锤否认（法定披露/本人表态）③ 相关标的已被传闻定价=回吐风险（A 闸天然成立）。案例：SK海力士×英特尔俄亥俄厂（DART 披露否认，10000002226）、富士康×SpaceX 520 亿订单（马斯克"fake news"，10000002228）。
- **建**：预览确认（不可跳过，确认前不调 MCP；**🤖 AUTO 模式除外**——跳预览但一律停 status=2，见 AUTO 协议）→ create（**不传 desc**；**keywords ≤4 个铁律 v2.7.4**——主标的 ticker+名 + 最核心 1-2 概念，砍次要实体/同义堆叠，别再塞 10+；**⚠️ keywords 必须传逗号分隔字符串 `"IREN,HUT8,比特币矿股"`，禁传数组 `["IREN",...]` — 铁律 v2.9.11**：数组会被序列化成字面量 `"[\"IREN\",...]"` 原样存库，**console 不报错=静默失败**，后果是打标器拿到带方括号转义引号的串必然匹配不到、`matched_tags` 永远为空。判据：`update` 返回的 `new_keywords` 里出现 `[` 或 `\"` 即是坏的，改传字符串重发。update 同理）→ **tag 校验（铁律 v2.9.5 — 系统打标器不可信，每轮必验）**：create 返回的 `matched_tags` 当场看——① **≥7 位长 ID = 系统自造垃圾占位**（正常 tag 5-6 位），force-set 正确 tag 或清空；② **错实体**（标题机构缩写误标：Circle→OCC、SpaceX→SPC 类）同覆盖；③ 无已知正确 tag → **留空**（宁空不错标）。改法 `update_trending_topic(tags=[{"id":..}])`（传 `tags` 以它为准、不被 keywords 顶掉）。新实测 tag 补进 data-quickref。（根因+实扫：LESSONS v2.9.5）→ **4.5 数据核实**（≥5 条走批量路径；地缘/政策/突发自动加 WebSearch 二次核 v2.5.3）。
  > **🔢 多数字标题必须逐个回溯到源（铁律 v2.9.10 — 2026-07-20 用户点名核查 10000002201 抓出）**：标题里每出现一个数字，**必须能指名道姓说出它来自哪一条源**；两个数字之间若被写成「A→B」「腰斩」「翻倍」等**变化关系**，那个关系本身也要有源直接支持，**不能由我拼接**。
  > - **典型错法**：把「霍尔木兹通行降至 **8** 艘（克普勒）」和「美军拦查 **4** 艘商船（CENTCOM）」拼成「通行量 8→4 腰斩」——**两个数字单看都真，但不是同一量纲，造出的"变化"不存在**。比单数字写错更隐蔽（逐字 fact-check 全过）。
  > - **自检一句话**：写完标题回头指着每个数字问「这个数出自哪一句？」，**指不出来的删掉**；「涨了多少 / 降了多少 / 破了什么」这类关系词尤其要问。
  > - **顺带**：单源数字与另一源冲突时（如布油周涨幅 Reuters 15.9% vs Economic Times ~12%）取**保守口径或直接不写**，别取上限；「N 周新高」这类需按最保守源折算（LESSONS v2.9.10）。
- **⚠️ status 真相（铁律 v2.5.6 — 2026-06-10 实测推翻 v2.5.2）**：`create_trending_topic` **返回值是 `status=2`，但系统后台会在几分钟内自动把它推到 `status=0` 上线**——**靠"不动"拦不住自动上线**。
  - **报告 status 一律以 `list_trending_topics` 实查为准，禁止拿 create 返回的 `status=2` 当最终态**（这是 v2.5.2 的误判根源：把 create 返回值当成了停驻态）。
  - **要"建完不上线/留审核"，必须建完立刻 `update_trending_topic(status=2)` 主动按住，并隔一会儿 re-list 复查**；若要**确保不出现在前台**，用 `status=3 隐藏`（比 2 更稳，2 仍可能被后台再推）。
  - ⏱️ **延时复查铁律（v2.9.4 — 2026-07-11 实测）**：系统自动推 status=2→0 有**数分钟延迟**，**建完立即 re-list 会读到还没被推的 status=2、误判"没被自动上线"**（本轮两条实测：09:14 建、09:20 查还是 2、后续查已变 0）。要确认最终态，**re-list 须隔 ≥3-5min**，或直接看下一轮 B0；别拿刚建完的即时查当"稳住了"的结论。
  - 🔒 **禁止主动推上线（铁律 v2.6.3，口径由 v2.9.17 更新）**：**禁止主动 `update status=0`**——发布不由我执行。~~（近期实测自建话题本就不会自动上线、停在 status=2）~~ **这句已被 v2.9.17 推翻，且它与上方 v2.5.6/v2.9.4 本就自相矛盾**，是我多次误判的根源。
  - 🚀 **新建话题约 5 分钟后自动上线（铁律 v2.9.17 — 2026-07-30 用户告知机制并拍板落库）**：我 create 的新话题**由系统在约 5 分钟后自动推 `status=0`**，我**什么都不用做**（既不用推、也拦不住）。三条连带后果：
    > - **🚫 「未上线所以不构成误导」这条推理彻底作废**——这是本条最硬的一点。此前我对前提被推翻的题（`10000002276` 美伊停火、`10000002325` SKHY 预告）反复用过「它全程 status=2 从未上线、不误导读者、故不急处置」放过自己。**新机制下建完即发布，前提被推翻的题必须当"已上线内容"处置**，不许再拿"没人看到"当挡箭牌。
    > - **⚠️ 自查从"有网兜"变成唯一防线**：`status=2` 不再是安全网（写错了没人拦）。数字逐个回溯（v2.9.10）、时点标注、口径交叉这三项**上线前必须一次做对**。
    > - **📈 退场复查负担上升**：我的题几乎都会进 v2.6.8「`status=0` 且 ≤6h」复查池，逐条过的量明显增加；误撤的代价也从"撤一条没露面的题"变成"**撤回已发布内容**"，健康线实质更严。
    > - **📌 实测口径不一致（如实标注，勿当稳定规律）**：并非所有题都会被推上线——`10000002350/10000002351/10000002373/10000002374` 分钟级上线，而 `10000002273`（07-27 10:08 建）、`10000002276`、`10000002349` **超 48h 仍停在 `status=2`**。5 分钟自动上线按用户告知**对新建题生效**，历史卡滞题不适用；**报告 status 一律以 `list_trending_topics` 实查为准**（v2.5.6 原则不变）。
- **📊 报告表格化优先（铁律 v2.7.9 — 2026-07-01 用户指令「output 最好表格化」；2026-07-06 复申并加⑤）**：所有报告输出**默认用表格承载**，减少长散文/多层 bullet 堆叠——① 三件套（🆕候选 ID/标题/评分/缺口、🔧更新 ID/现状/改为、🚨撤回 ID/原因）用表；② 价格快照（标的/价/涨跌）用表；③ 已执行处置（ID/动作/结果）用表；④ 当前话题全列（ID/标题/status）用表；⑤ **待决策/待你拍板事项用表**（列 事项/内容/建议，多条积压逐条一行，别散在段落）。**一句话结论 + 表格**，能表格化就别写段落；仅纯叙述性判断可少量散文。AUTO 模式尤其严格（无人复核）。
- **🚦 通道齐备闸 — 报告前强制自查（铁律 v2.9.14 — 2026-07-23/24 连续两日同型违规，用户两次追问「怎么跑得这么快/数据跑齐了吗」逼出）**：**出报告前必须先在报告里列出本轮实跑通道清单**，逐项打 ✅/❌，未跑齐则**禁止下任何全景结论**。
  > | 必查项 | 首扫 | 刷新 |
  > |---|---|---|
  > | B0 console（含满额守卫）| ✅必 | ✅必 |
  > | 价格 10币 + 金/油/美元/指数 | ✅必 | ✅必 |
  > | news 事件面（漏洞/上币/解锁/SEC）| ✅必 | ✅必 |
  > | okx **涨榜 + 跌榜 + OI** 三件套 | ✅必 | ✅必 |
  > | A1 三栈 twitter 子进程 | 3栈 | 2栈 |
  > | TG exploit 探针（或熔断兜底）| ✅必 | ✅必 |
  > | 板块矩阵 6批×5 | ✅必（受时段闸）| 按需 |
  > | 国债 / econ / earnings 日历 | ✅必 | 砍 |
  >
  > - **🚫 未跑齐禁下的结论类型**：「池子已被系统盖满/我方缺口只有 N 条」「今日主线是 X」「干净干轮/无候选」「A 是 B 的原因」——这些**全需要全景视野**，通道缺项时一律改写成「**已跑通道内**未见其他缺口」并标 ⚠️ 半盲。
  > - **📌 根因（写死在这里，因为写进 actions 不管用——连续两天违规都发生在我意识到之前）**：**注意力被单一大事件捕获后，"这条线查透了"会被误当成"全局看完了"**。07-23 被谷歌财报捕获→漏掉 AFX \$2415万黑客；07-24 被 CLARITY 捕获→漏掉布伦特破\$100（且把 BTC 跌破 65K 错误归因给 CLARITY，实为油价+美债收益率，Cointelegraph/eliant_capital 双源打脸）。**大事件日恰恰是最该跑齐通道的日子，不是可以省的日子。**
  > - **执行位点**：本闸位于「收尾必列完整清单」之前——**先自查通道、再写三件套**，顺序不可倒。

- **收尾必列完整清单（铁律 v2.5.5 + v2.5.9 当前话题全列 + v2.9.6 去重审计）**：每轮扫描**结尾固定输出**，不口头带过、不省略。六块：
  1. **三件套全列**：🆕 候选（评分+缺口）｜🔧 待更新｜🚨 待撤/在审处置 —— 干轮如实写"无"，不灌水不硬报 0
  2. **本轮已执行**：建了哪些（ID + **list 实查 status**，别拿 create 返回值；多半已自动上线，见 v2.5.6）/ 改了哪些 / 撤了哪些
  3. **⏸ 待用户决定**：待批更新 / 待撤；若用户曾要求"留审核"，复查这些是否被自动上线、需不需要 update status=2/3 按回。**⏰ 积压标记**：同一待决项跨 2 轮未决 → 标 "⏰积压N轮"；有客观死线的（如"今晚上市"）**置顶标死线 + 给默认建议**，过线后降级为"已过期，不再提"
  4. 不确定的别塞进"已执行"，归到"待决定"
  5. **📋 当前话题完整 list（铁律 v2.5.9 — 2026-06-12 用户反复要"把 list 列清楚"，不止决策项）**：每轮收尾**必附一份基于 B0 实查的当前话题全列**——**按 `domain`(crypto/tradfi) + `status`(0上线/2审核/3隐藏) 分组，把今日相关话题逐条 ID+标题列全**，不是只列三件套涉及的几条。这是"撤改补"之外用户要的「现在线上到底有哪些话题」的全景，**禁止省略成"系统在审一批待你定"一句话带过**。干轮也要列（行情清淡时全 list 照列）。
  6. **🧩 全池去重审计（铁律 v2.9.6 — 2026-07-15 用户指令，收尾固定一节）**：收尾**必摊全池 status=0+2**（不止 ≤8h 窗）跑一遍 §2 v2.7.3+v2.9.6 检测，报告列清三类：**① 已即时合并/下架的**（≤6h 同事件、单名vs板块冗余、盘中反转错向老题——逐条 ID+留哪条撤哪条）；**② 只能交 dev 的**（同名多条刷屏、命中多条无 ID 入口）；③ 干净无重复也明写"全池无重复/反向"。禁止像 2026-07-15 首扫那样只摊管理窗、把 >8h 碎片簇一句"交 dev"带过。

---

## 🤖 AUTO 模式 — 无人值守协议（v2.8.1 — 2026-07-03 定时自动扫描）

> **触发**：定时任务（cron/routine）以 prompt 含 `AUTO` 字样调起本 skill 时进入本模式；用户在场的交互轮**不适用**（照常走意图闸/预览确认）。模式判定同 §0（v2.8.8）：**当天第一轮=首扫(24h)、其余=刷新(4h)**，以 `last-refresh.txt` 跨天判定。
> **轮内执行序照 §2 铁律（v2.8.5）**：①扫热点 → ②盘 8h 池 → ③处置存量（改/合并/撤）→ ④最后补题——AUTO 尤其禁止一上来就建题。

### 自主决策边界（超出即跳过并在报告标注，不猜）

| ✅ 允许自动执行 | 🚫 禁止自动执行 |
|---------------|---------------|
| **建题 ≤3 条/轮**：仅 P0/P1 且过全部闸（行情关联度 A≥2、链上/事实红线、样式标准 v2.8.0、keywords≤4 v2.7.4、tag 核验 v2.7.5），**跳过预览确认但一律停 `status=2` 审核** | **推 `status=0` 上线**（永远人工；发布权在团队）|
| ≤6h 内容重复主动合并（v2.7.3）；**全池去重审计（v2.9.6）**：收尾摊全池、≤6h 合并 + **盘中反转错向老题下架**（收盘方向反转=客观事实反转，同 v2.9.4 破 8h 闸，AUTO 可自动执行）| 批量撤 >10 条（列清单进报告等人拍板）；**>6h「单名 vs 板块冗余」合并留哪条=判断项 → AUTO 跳过标 🟡**（哪条更全需人判，非客观）|
| ≤6h 审核池质量复审撤（v2.7.7）| 修改 SKILL.md / LESSONS / git commit（规则演进必须有用户在场）；**撤 meme 币**——除「同标的重复/已证伪/事实错误」三类外**一律禁撤（v2.9.7 默认保留）**，拿不准就留着标 🟡 |
| ≤6h 上线退场复查（v2.6.8，受 8h 闸/时段闸约束）| 对 >8h 话题做任何操作（v2.7.1 照旧）|
| **tag 复核（v2.9.5 — 用户指令「每天跑 AUTO 时帮忙 review 一下 tag」）**：① 自建题 create 后当场验 `matched_tags`，垃圾长 ID/错实体即 force-set 或清空；② 处置 ≤8h 系统批时**顺手**按速查表 force-set 已知 tag（tag 卫生同碎片检测，**不受 8h 闸豁免**）| — |
| tradfi 时段闸内改价（v2.7.8）| 模糊判定（去重边界/幅度口径打架/单源硬定义词）→ 跳过，报告标 🟡 |

### 每轮固定输出（无人也要留档）
0. **🚦 通道齐备自查表（v2.9.14，第一顺位）**：逐项 ✅/❌ 列本轮实跑通道；未跑齐禁下全景结论、改标 ⚠️ 半盲。AUTO 无人复核，此闸比手动轮更硬。
1. 表格化三件套日报（v2.7.9）+ 本轮自动执行清单（建/合并/撤，逐条 ID）
2. 🟡 跳过项清单（为什么跳过、等人决策什么）
3. 干轮如实报干轮，禁止为"显得有产出"而降闸建题
4. **收尾双留档必写**（§0 v2.8.3：B0 快照 + actions 日志）——AUTO 模式漏写留档 = 效果评估断档
5. **半盲熔断**（§0 v2.8.4）：news/TG/三栈 任一失败且降级补不齐 → 报告首行标「⚠️ 半盲扫·结论存疑」+ 列缺失通道；**禁报"干净干轮"**，actions 留档标 note

### 状态持久化
- 时间戳文件在 **`$HOME/.trend-scout/last-refresh.txt`**（v2.8.1 从 `/tmp` 迁出——`/tmp` 会被系统清理，实测一个会话内被清 3 次，对 cron 是致命的；`$HOME` 下跨重启存活）。开跑 `mkdir -p ~/.trend-scout` 兜底。
- 三角校验铁律（v2.5.4）在 AUTO 模式**更关键**：headless 环境时钟/上下文更易漂移，date × currentDate × live 数据必须互证后才动手。

---

## 🎯 管线与理念（v2.2，压缩版 v2.9.9）

入口 **A 扫描 / B 素材 / C 定向** → 0a 拉已建索引 → 0a.5 在审池预过滤 → 0b 四维评分 → **主报告 = 撤改补三件套**（🆕补充最优先 → 🔧更新 → 🚨撤回，附已建覆盖+实时数据）→ 字段提取/标题 → 预览（AUTO 免）→ create → tag 校验 → 数据核实 → status 以 list 实查为准（要留审核须主动按住，v2.5.6）。

**风向标本质 = 帮用户决定"今天该补什么话题"**。🆕 不设数字 KPI（v2.8.2）：全源扫完、过闸如实报，0 条即干轮；唯一自检是**覆盖**——大事日 0 候选先对照 §1 查通道漏没漏，无漏即真干轮，禁扩窗凑数抬 P2 进 P0/P1。

---

---

## 📚 详章索引（按需加载，执行卡是单一事实源）

| 文件 | 内容 | 何时读 |
|------|------|--------|
| `references/scan-playbook.md` | 第 -1/-1.5 步详章：时间校准、followin quirks 13 条、TG exploit 探针范式、板块异动判定、宏观符号 fallback、Wave5 query、刷新模式、推特 L1 双轨排序、子进程 prompt 模板、候选池兜底 | 扫描中遇到边界判定/调用失败时 |
| `references/dedup-scoring.md` | 第 0a/0a.5/0b 步详章：去重索引、升温硬规则、审核池预过滤、四维评分细则（A 维含行情关联度闸）、一票否决、三张表输出模板 | 评分/处置判定拿不准时 |
| `references/fields-style.md` | 第 1-5 步详章：字段提取、标题样式标准 v2.8.0 全表、中英混杂规则、keywords/domain/topic_type、预览→create→tag 校验→数据核实→发布、紧急撤回 | 建题字段拿不准时 |
| `references/data-quickref.md` | **数据表**：三栈 list ID、10 币表、板块矩阵 8×4、宏观符号映射、tag id 速查、update 用法 | 查数据时（数据更新只动这个文件）|
| `references/weekly-review.md` | 周复盘效果评估：采纳率/打回率/误撤率/返工率算法 + 周报模板（吃 §0 双留档数据）| 用户喊"周复盘/效果复盘"或 AUTO 周任务 |
| `LESSONS.md` | 完整事故档案 + 版本演进表 | 复盘/溯源时 |

> **架构约定（v2.8.2）**：SKILL.md 只放「每轮必用」的执行卡+铁律+AUTO 协议；判定细节进 references；**数据与规则分离**（tag/list/币表过时只改 data-quickref，不动规则）；新教训入 LESSONS + 版本表，规则改动落到对应文件，**不再往本文件堆补丁**。
